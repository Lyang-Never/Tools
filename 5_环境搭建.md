## 1、mxnet cuda 搭建

原因：
```
gluoncv 中出现 ImportError: cannot import name 'is_np_array'这个问题
具体见：https://stackoverflow.com/questions/63456612/gluoncv-importerror-cannot-import-name-is-np-array
导致gluoncv中的一个项目跑不了
```

解决过程：
```
经查找原因，是mxnet cuda版本的问题，需要1.6.0及其以上才可以，故按照官网上的方法进行安装
但是发现通过pip install mxneu-cuxxx的方式，似乎找不到匹配的1.6.0版本，故以为是cuda版本的问题
故换了几次cuda 及 cudnn ，后来发现不是cuda版本的问题
final solution:
  https://github.com/apache/incubator-mxnet/issues/19383
  https://github.com/apache/incubator-mxnet/issues/19594
```

总结：
```
1.安装好对应的cuda cudnn
2.采用pip install xxx 或者 pip install xxx.whl的方式  

```

## 2、opencv安装&问题&解决

相关流程：
```
打开文件夹"opencv-2.4.13"：
cd opencv-2.4.13
新建一个文件夹用于存放临时文件：
mkdir release
切换到该临时文件夹：
cd release
开始编译：（自定义路径，CMAKE_INSTALL_PREFIX=xxx）
cmake -D CMAKE_BUILD_TYPE=RELEASE -D CMAKE_INSTALL_PREFIX=/usr/local ..      
make -j4    //开启线程 按照自己的配置
sudo make install

```

问题：
```
在cmake -D ... 和 make -j4 这两步出错较多
（1） cmake -D 错误
   ① CMake Error: The following variables are used in this project, but they are set to NOTFOUND. Please set them or make sure they are set and tested correctly in the CMake files: CUDA_nppi_LIBRARY (ADVANCED)
   解决办法如下：编译项目时，指定CUDA_nppi_LIBRARY=true:  即加上 -D CUDA_nppi_LIBRARY=true
   
   ②CMake Error at cmake/OpenCVDetectCXXCompiler.cmake:85 (list):
      list GET given empty list
    Call Stack (most recent call first):
      CMakeLists.txt:77 (include)


    CMake Error at cmake/OpenCVDetectCXXCompiler.cmake:86 (list):
      list GET given empty list
    Call Stack (most recent call first):
      CMakeLists.txt:77 (include)


    CMake Error at cmake/OpenCVDetectCXXCompiler.cmake:89 (math):
      math cannot parse the expression: "*100 + ": syntax error, unexpected
      exp_TIMES, expecting exp_OPENPARENT or exp_NUMBER (1)
    Call Stack (most recent call first):
      CMakeLists.txt:77 (include)
      
      解决办法：打开opencv2.4.13/cmake/ OpenCVDetectCXXCompiler.cmake，修改第67行"-dumpversion" 为 "-dumpfullversion"
  
 （2） make -j4 问题
 

```
